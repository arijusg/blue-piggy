import { assert } from "chai";

describe("Path Hero", () => {

    before(async () => {
    });

    after(async () => {
    });

    it("should be awesome", async () => {
        assert.equal("awesome", "awesome");
    });
});